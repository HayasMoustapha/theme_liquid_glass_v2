<!-- ai-dev-system: managed CLAUDE.md -->
# Project onboarding for Claude

This project runs under the **AI Dev System** (folder `.ai-dev-system/`). Before acting:

1. read `AGENTS.md` (the project-local adapter and mandatory baseline);
2. read `.ai-dev-system/PROJECT-LOCAL-ADAPTER.md` and `.ai-dev-system/host-adapters/claude-code/session-checklist.md`;
3. if a Governor session exists under `.ai-dev-system/governor/sessions/`, open its `START-HERE.md` and work strictly within that session's artifacts;
4. never bypass the system: use the `/ads-*` commands and go through Governor.

Golden rule: **never mark as done what is not proven**, and stay inside the active Governor session.

Common commands (Claude may also write them without the leading slash):

```text
/ads-check                     # health-check the system in this project
/ads-go <besoin>               # run a Governor-framed task
/ads-governor-init <besoin>    # create/open a Governor session
/ads-governor-next             # prepare the next executable slice
/ads-capability-scout --need "..."   # find the best web resources for a need
/ads-capability-acquire --start "..." # governed acquisition (scout->review->install->verify)
```

This file is generated by the AI Dev System. Edit `AGENTS.md` or the adapter for lasting project rules.
