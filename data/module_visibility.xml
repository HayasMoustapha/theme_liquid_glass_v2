<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!--
        Avoid overriding the global Apps action (base.open_module_tree).

        That kind of override is brittle and can make modules "disappear" depending on
        context/search defaults. Module visibility in Apps should be driven by the
        addon manifest (application/installable) and normal module list refresh.
        -->
    </data>
</odoo>
